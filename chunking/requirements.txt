# requirements.txt
# chunking パッケージの依存関係
 
# Ollama 用クライアント（LLM: チャンク分割 / 3段階セマンティックチャンキング）
# Ollama は OpenAI 互換 API（/v1）を公開するため openai SDK を使用（ローカル実行・API キー不要）
openai>=1.30.0
 
# Data validation
pydantic>=2.0.0
 
# Progress bar
tqdm>=4.65.0
 